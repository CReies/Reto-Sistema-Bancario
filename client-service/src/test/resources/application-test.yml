spring:
  r2dbc:
    url: r2dbc:h2:mem:///clients_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: false

app:
  messaging:
    enabled: false
  rabbitmq:
    listener:
      simple:
        auto-startup: false
